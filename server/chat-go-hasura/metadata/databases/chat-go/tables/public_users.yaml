table:
  name: users
  schema: public
array_relationships:
  - name: chats
    using:
      foreign_key_constraint_on:
        column: receiver_id
        table:
          name: chats
          schema: public
  - name: chatsBySenderId
    using:
      foreign_key_constraint_on:
        column: sender_id
        table:
          name: chats
          schema: public
